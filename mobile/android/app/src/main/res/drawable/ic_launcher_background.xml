<?xml version="1.0" encoding="utf-8"?>
<!-- Solid navy — matches the logo asset's own baked-in background
     color exactly (sampled: #01225E) so the adaptive icon's
     background layer is seamless with the foreground layer's logo,
     regardless of which mask shape (circle/squircle/rounded square)
     the device's launcher applies. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportHeight="108"
    android:viewportWidth="108">
    <path
        android:fillColor="#01225E"
        android:pathData="M0,0h108v108h-108z" />
</vector>
